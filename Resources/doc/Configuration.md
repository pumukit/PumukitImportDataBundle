PumukitImportDataBundle Configuration
=====================================

Execute the following command to initialize videos

```bash
php app/console pumukit:data:import --file="{pathToFile}"
```

Where {pathToFile} is the absolute path to the file.
